import React from 'react';

/**
 * KanbanBoard component
 *
 * - Displays tasks in a Kanban board layout (columns by status).
 * - Supports drag-and-drop to move tasks between columns.
 *
 * Props and internal state:
 *  - tasks: TaskRead[]
 *  - onTaskMove: (taskId: string, newStatus: string) => void
 *
 * TODO:
 *  - Implement using react-beautiful-dnd or similar.
 *  - Integrate with API (src/api/tasks) and React Query.
 */

export function KanbanBoard() {
  // TODO: Implement Kanban board UI
  return null;
}
