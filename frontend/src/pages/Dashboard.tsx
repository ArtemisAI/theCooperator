import React from 'react';

/**
 * Dashboard page
 *
 * - Fetches and displays high-level metrics (total members, tasks summary, vote stats).
 * - Uses API hooks from src/api/metrics.
 * - Composes multiple components: charts, tables, widgets.
 *
 * TODO:
 *  - Implement data fetching with React Query.
 *  - Layout widgets using MUI Grid or similar.
 */

export default function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>
      {/* TODO: Add dashboard widgets */}
    </div>
  );
}
